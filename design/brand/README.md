## Note

The PNG files (`mark-transparent.png`, `logo-square-dark.png`, `logo-horizontal-dark.png`) contain the previous VANTAGE wordmark and need new versions with "MAX" branding. The `mark.svg` is a pure shape (no text) and does not need changes.